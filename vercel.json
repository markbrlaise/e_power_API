{
    "version": 2,
    "name": "e_power_api",
    "buildCommand": "next build",
    "builds": [
        {
            "src": "app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "app.js" }
    ]
}
